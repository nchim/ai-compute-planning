{
  "name": "capplanner-harness",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright remote-control harness + acceptance scripts (WS9/WS10). Placeholder until WS9 lands.",
  "type": "module",
  "scripts": {
    "test": "echo 'harness: no tests yet (WS9)' && exit 0"
  }
}
